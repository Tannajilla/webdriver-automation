import Contextmenu from "../pageobjects/Contextmenu";
describe("test script",async function ( ) {

    it("checkbox testcase", async function ( ) {

        await Contextmenu.open();
        await browser.pause(5000);
        
    })
})